name: oracle
description: "Oracle — 일상 디스커버리 채팅 동반자 + 비서"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  intl: ^0.20.2
  flutter_markdown: ^0.7.4
  shared_preferences: ^2.3.3
  camera: ^0.11.0
  permission_handler: ^11.3.1
  speech_to_text: ^7.0.0
  record: ^5.1.2
  path_provider: ^2.1.4
  image_picker: ^1.1.2
  receive_sharing_intent: ^1.8.1
  flutter_local_notifications: ^17.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
